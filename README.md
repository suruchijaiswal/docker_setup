# Dockerized setup for RMA Microservices + API Gateway (Fixed)

## Expected layout
```
project-root/
├── rma_java_microservices/
├── api-gateway-main/
└── docker-setup-fixed/   
```

## Run
```
cd docker-setup-fixed
docker compose build
docker compose up -d
```

